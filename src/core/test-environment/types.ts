export enum NetworkOptions {
  Testnet = "testnet",
  Futurenet = "futurenet",
  Local = "local",
}

export enum ResourceLimitOptions {
  Default = "default",
  Testnet = "testnet",
  Unlimited = "unlimited",
}
